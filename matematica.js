//var numero = prompt("numero");
//alert (Math.sqrt(numero));
 if (Math.asin(60) == Math.sqrt(1.73)/2)
  alert ("iguais");
else
  alert ("diferente");
  //operador ternário;
 alert ((Math.asin(60)== Math.sqrt(1.73)/2)? "iguais" : "diferente");